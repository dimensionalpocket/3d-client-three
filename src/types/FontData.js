/**
 * @typedef {object} FontData
 *
 * @property {string} id - ID/name of the font to use in FontLibrary.
 * @property {string} url - URL of the JSON file with font definition.
 * @property {id} textureId - ID of the texture to use; must have been added to client beforehand.
 */
