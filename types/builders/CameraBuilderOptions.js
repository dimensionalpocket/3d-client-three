/**
 * @typedef {object} CameraBuilderOptions - options for building THREE Cameras.
 *
 * @property {id} id - Camera ID
 * @property {string} [type] - Default "perspective"
 * @property {number} [aspect] - Aspect ratio. Default 1.
 * @property {number} [fov] - Field of View. Default 50.
 * @property {number} [near] - Min distance to render objects. Default 0.01.
 * @property {number} [far] - Max distance to render objects. Default 1000.
 */
